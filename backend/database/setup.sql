-- LSP Digital Database Setup
-- Run this script to create the database

CREATE DATABASE IF NOT EXISTS lsp_digital 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Grant privileges (adjust username as needed)
-- GRANT ALL PRIVILEGES ON lsp_digital.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;

USE lsp_digital;
